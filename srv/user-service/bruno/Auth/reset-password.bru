meta {
  name: Reset Password
  type: http
  seq: 5
}

post {
  url: {{base_url}}/auth/reset-password?token=your_reset_token&newPassword=newpass123
  body: none
  auth: none
}

params:query {
  token: your_reset_token
  newPassword: newpass123
}
